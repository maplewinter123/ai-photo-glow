# 阿里云正式部署步骤

1. 域名：准备自己的域名，建议 www.你的域名.com 做前端、api.你的域名.com 做后端。若资源在中国内地，绑定域名提供网站访问前需按规定完成 ICP 备案。
2. 百炼：创建 DASHSCOPE_API_KEY。
3. 函数计算 FC：创建 Web函数，上传 backend，启动命令 npm run start，监听端口 9000。
4. 环境变量：DASHSCOPE_API_KEY、QWEN_MODEL=qwen-vl-plus、DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1、FRONTEND_ORIGIN=*。
5. 创建 HTTP 触发器，测试 /health。
6. 打开 frontend/index.html，把 window.API_BASE 改成 FC 公网地址。
7. OSS：创建 Bucket，开启静态网站托管，默认首页 index.html，上传 frontend/index.html。
8. 绑定 www 域名到 OSS，api 域名到 FC，并配置 HTTPS。
9. 支付：取得支付宝/微信支付商户参数后，完善 /api/create-order 与 /api/payment-notify。必须做验签、金额核对、订单持久化和防重复发货。
10. 广告：网站内容、域名、隐私政策、备案和流量稳定后再申请广告平台。
11. 后续升级顺序：登录→免费次数→订单数据库→支付→会员→分享卡→广告→CDN→限流风控→数据分析。
